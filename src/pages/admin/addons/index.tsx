import { useEffect, useState } from "react"; import { Card, CardContent, CardHeader, CardTitle, CardDescription } from "@/components/ui/card"; import { AdminPageShell } from "@/components/admin/AdminPageShell"; import { getAllAddons } from "@/services/adminService";
export default function AdminAddonsRoute(){ const [addons,setAddons]=useState<any[]>([]); const [error,setError]=useState(""); useEffect(()=>{getAllAddons().then(setAddons).catch((err)=>setError(err.message||"Failed to load add-ons"));},[]); return <AdminPageShell title="Add-ons" description="Add-on catalog and productization layer for WOF and future modules."><Card><CardHeader><CardTitle>Add-on catalog</CardTitle><CardDescription>Current DB-backed add-ons.</CardDescription></CardHeader><CardContent className="space-y-3 text-sm">{error?<div className="text-red-600">{error}</div>:null}{addons.map((addon)=><div key={addon.id} className="rounded-lg border p-3"><div className="font-medium">{addon.display_name}</div><div className="text-muted-foreground">{addon.addon_type} · Monthly: ${Number(addon.price_monthly||0).toFixed(2)} · Annual: ${Number(addon.price_annual||0).toFixed(2)}</div></div>)}</CardContent></Card></AdminPageShell>}
