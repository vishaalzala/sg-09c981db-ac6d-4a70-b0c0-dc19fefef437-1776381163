import { useEffect } from "react"; import { useRouter } from "next/router";
const legacyTabRedirects: Record<string, string> = { dashboard: "/admin/dashboard", control: "/admin/dashboard", revenue: "/admin/billing", companies: "/admin/companies", users: "/admin/companies", notifications: "/admin/communications", messaging: "/admin/communications", leads: "/admin/support", plans: "/admin/plans", addons: "/admin/addons", roles: "/admin/security", audit: "/admin/audit", reports: "/admin/dashboard", settings: "/admin/settings" };
export default function AdminIndexPage() { const router = useRouter(); useEffect(() => { const tab = typeof router.query.tab === "string" ? router.query.tab : "dashboard"; router.replace(legacyTabRedirects[tab] || "/admin/dashboard"); }, [router]); return null; }
