import { useEffect, useState } from "react"; import { Card, CardContent, CardHeader, CardTitle, CardDescription } from "@/components/ui/card"; import { AdminPageShell } from "@/components/admin/AdminPageShell"; import { getAuditLogs } from "@/services/adminService";
export default function AdminAuditRoute(){ const [logs,setLogs]=useState<any[]>([]); const [error,setError]=useState(""); useEffect(()=>{getAuditLogs({limit:100}).then(setLogs).catch((err)=>setError(err.message||"Failed to load audit logs"));},[]); return <AdminPageShell title="Audit" description="Cross-platform audit trail for admin and SaaS lifecycle actions."><Card><CardHeader><CardTitle>Audit log</CardTitle><CardDescription>This route keeps the audit trail out of the old all-in-one admin page.</CardDescription></CardHeader><CardContent className="space-y-3 text-sm">{error?<div className="text-red-600">{error}</div>:null}{logs.length?logs.map((log)=><div key={log.id} className="rounded-lg border p-3"><div className="font-medium">{log.entity_type} · {log.action_type}</div><div className="text-muted-foreground">{log.created_at}</div><div className="text-muted-foreground break-all">Entity ID: {log.entity_id||"—"}</div></div>):<div className="text-muted-foreground">No audit rows found.</div>}</CardContent></Card></AdminPageShell>}
