import { Card, CardContent, CardHeader, CardTitle, CardDescription } from "@/components/ui/card"; import { AdminPageShell } from "@/components/admin/AdminPageShell";
export default function AdminChangelogRoute(){ return <AdminPageShell title="Changelog" description="Admin-owned product release notes page, ready for a future changelog table."><Card><CardHeader><CardTitle>Ready for implementation</CardTitle><CardDescription>I added the route and navigation so release notes can live inside the admin instead of another external tool.</CardDescription></CardHeader><CardContent className="space-y-2 text-sm text-muted-foreground"><div>Suggested schema next: changelog_posts(id, title, body, audience, published_at, created_by).</div><div>Suggested tenant UI next: in-app “What’s New” drawer or banner.</div></CardContent></Card></AdminPageShell>}
