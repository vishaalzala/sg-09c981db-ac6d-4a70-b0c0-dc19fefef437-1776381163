import { Card, CardContent, CardHeader, CardTitle, CardDescription } from "@/components/ui/card"; import { AdminPageShell } from "@/components/admin/AdminPageShell";
export default function AdminApiKeysRoute(){ return <AdminPageShell title="API Keys" description="Future tenant integration surface for key issuance, rotation, and revocation."><Card><CardHeader><CardTitle>Schema not present yet</CardTitle><CardDescription>The current database types do not include a tenant API key table, so I left this as a route placeholder instead of wiring fake data.</CardDescription></CardHeader><CardContent className="space-y-2 text-sm text-muted-foreground"><div>Suggested next table: tenant_api_keys(id, company_id, label, key_prefix, hashed_secret, scopes, last_used_at, revoked_at).</div><div>Suggested controls: create, rotate, revoke, view last used, per-scope permissions.</div></CardContent></Card></AdminPageShell>}
