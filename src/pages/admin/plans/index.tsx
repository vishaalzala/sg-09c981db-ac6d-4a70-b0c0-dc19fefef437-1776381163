import { useEffect, useState } from "react"; import { Card, CardContent, CardHeader, CardTitle, CardDescription } from "@/components/ui/card"; import { AdminPageShell } from "@/components/admin/AdminPageShell"; import { getAllPlans } from "@/services/adminService";
export default function AdminPlansRoute(){ const [plans,setPlans]=useState<any[]>([]); const [error,setError]=useState(""); useEffect(()=>{getAllPlans().then(setPlans).catch((err)=>setError(err.message||"Failed to load plans"));},[]); return <AdminPageShell title="Plans" description="DB-driven subscription catalog. This replaces hardcoded plan assumptions."><Card><CardHeader><CardTitle>Plan catalog</CardTitle><CardDescription>These are the plans currently stored in subscription_plans.</CardDescription></CardHeader><CardContent className="space-y-3 text-sm">{error?<div className="text-red-600">{error}</div>:null}{plans.map((plan)=><div key={plan.id} className="rounded-lg border p-3"><div className="font-medium">{plan.display_name||plan.name}</div><div className="text-muted-foreground">Monthly: ${Number(plan.price_monthly||0).toFixed(2)} · Annual: ${Number(plan.price_annual||0).toFixed(2)}</div></div>)}</CardContent></Card></AdminPageShell>}
