import { useEffect, useState } from "react"; import { Card, CardContent, CardHeader, CardTitle, CardDescription } from "@/components/ui/card"; import { AdminPageShell } from "@/components/admin/AdminPageShell"; import { getLeadsData } from "@/services/adminService";
export default function AdminSupportRoute(){ const [data,setData]=useState<any|null>(null); const [error,setError]=useState(""); useEffect(()=>{getLeadsData().then(setData).catch((err)=>setError(err.message||"Failed to load leads/support data"));},[]); return <AdminPageShell title="Support & Leads" description="Customer follow-up queue, leads, and the base for future ticketing."><Card><CardHeader><CardTitle>Current lead queue</CardTitle><CardDescription>This reuses your existing leads pipeline so support has one working route today.</CardDescription></CardHeader><CardContent className="space-y-3 text-sm">{error?<div className="text-red-600">{error}</div>:null}{data?.leads?.length?data.leads.map((lead:any)=><div key={lead.id} className="rounded-lg border p-3"><div className="font-medium">{lead.name || lead.companyName || "Unnamed lead"}</div><div className="text-muted-foreground">{lead.email || "No email"} · {lead.status}</div><div className="text-muted-foreground">{lead.message || "No message"}</div></div>):<div className="text-muted-foreground">No leads found.</div>}</CardContent></Card><Card><CardHeader><CardTitle>Next support features to build</CardTitle><CardDescription>These are not in the current schema yet, so I left them out of live queries.</CardDescription></CardHeader><CardContent className="space-y-2 text-sm text-muted-foreground"><div>• Ticket table with priority, assignee, SLA, and company linkage</div><div>• Impersonation banner with reason capture</div><div>• Feature request ledger tied to MRR</div><div>• Churn-risk and onboarding adoption nudges</div></CardContent></Card></AdminPageShell>}
